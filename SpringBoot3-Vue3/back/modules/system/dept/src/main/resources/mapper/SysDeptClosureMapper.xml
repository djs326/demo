<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.system.dept.mapper.SysDeptClosureMapper">
    
    <select id="selectDescendantIds" resultType="java.lang.Long">
        SELECT descendant_id FROM sys_dept_closure WHERE ancestor_id = #{ancestorId}
    </select>
    
    <select id="selectAncestorIds" resultType="java.lang.Long">
        SELECT ancestor_id FROM sys_dept_closure WHERE descendant_id = #{descendantId}
    </select>
    
    <insert id="insertClosureRecords">
        INSERT INTO sys_dept_closure (id, ancestor_id, descendant_id, distance)
        VALUES 
        (#{ancestorId}, #{ancestorId}, #{descendantId}, #{distance})
    </insert>
    
    <delete id="deleteByDescendantId">
        DELETE FROM sys_dept_closure WHERE descendant_id = #{descendantId}
    </delete>
    
</mapper>
